sitemaps:
  # Main sitemap with all content
  default:
    source: /query-index.json
    destination: /sitemap.xml
    lastmod: YYYY-MM-DD
    
  # Category-specific sitemaps for better organization
  media-releases:
    source: /media-releases.json
    destination: /sitemap-media-releases.xml
    lastmod: YYYY-MM-DD
    
  speeches:
    source: /speeches.json
    destination: /sitemap-speeches.xml
    lastmod: YYYY-MM-DD
    
  qantas-responds:
    source: /qantas-responds.json
    destination: /sitemap-qantas-responds.xml
    lastmod: YYYY-MM-DD
    
  roo-tales:
    source: /roo-tales.json
    destination: /sitemap-roo-tales.xml
    lastmod: YYYY-MM-DD
    
  gallery:
    source: /gallery.json
    destination: /sitemap-gallery.xml
    lastmod: YYYY-MM-DD